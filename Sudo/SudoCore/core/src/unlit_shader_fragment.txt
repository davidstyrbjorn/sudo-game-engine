#version 330 core

out vec4 FragColor;

in vec3 out_color;
in vec2 out_tex_coord;

uniform sampler2D ourTexture;

void main() 
{
	FragColor = vec4(out_color,1.0f);
	//FragColor = vec4(0.0f, 1.0f, 0.0f, 1.0f);
	//FragColor = texture(ourTexture, out_tex_coord);
}