#version 330 core

out vec4 FragColor;

in vec3 out_color;
in vec2 out_texCoords;

uniform sampler2D myTexture;

void main() 
{
	//if(out_textureMode == 0)
	//FragColor = vec4(out_color,1.0f);
	//if(out_textureMode == 1)
	FragColor = texture(myTexture, out_texCoords);
}